import { PlazaCreateForm } from "../components/plaza/PlazaCreateForm";

// 새 광장을 만드는 폼 페이지입니다.
export function PlazaNewPage() {
  return (
    <main className="px-5 py-8">
      <PlazaCreateForm />
    </main>
  );
}
